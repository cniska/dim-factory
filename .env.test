# `bun test` loads this before any test runs, so every git a fixture spawns reads no global or
# system config: a fixture commit that reached for the reader's signing key would fail inside a
# station worker's sandbox, which cannot reach the SSH agent.
GIT_CONFIG_GLOBAL=/dev/null
GIT_CONFIG_NOSYSTEM=1
