{
  "type": "object",
  "additionalProperties": false,
  "required": ["body", "slices"],
  "properties": {
    "body": { "type": "string", "minLength": 1 },
    "slices": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["title", "outcome"],
        "properties": {
          "title": { "type": "string", "minLength": 1 },
          "outcome": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
